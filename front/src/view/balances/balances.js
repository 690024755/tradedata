var React = require('react');var ReactDOM = require('react-dom');import config from '../../config/config.json';export default class Balances extends React.Component{    handleSearch(){        const mobile = this.inputMobileValue.value;        const amount = this.inputAmountValue.value;        const formData = new FormData();        formData.append("mobile",mobile);        formData.append("amount",amount);        const requestBody=new URLSearchParams(formData).toString()        const url = config.modifyBalance;        const opts = {            mode: 'cors',            method:"POST",            body:requestBody,   //请求体            headers: {                'Accept': 'application/json',                'Content-Type': 'application/x-www-form-urlencoded; charset=utf-8',            },        };        fetch(url, opts).then(res => res.text())            .then(data => {                const resData=JSON.parse(data);                console.log("data=" +JSON.stringify(data));                if(                    resData.success                ){                    //alert(resData['successInfo']);                    alert(resData.successInfo);                }else {                    //alert(resData['errorMessage']);                    alert(resData.errorMessage);                }            }).catch(e => console.log('错误:', e))    }    render(){        return(            <div>                <div>                    <label >手机号码：<input style={{"width":'99px', "height":'20px'}} id="mobile" type="text" ref={mobileValue => this.inputMobileValue = mobileValue} defaultValue="13093863511"                    />测试在用的小B用户:15356170982,13699512634,13093863511</label>                </div>                <div>                    <label >金额：<input style={{"width":'100px', "height":'20px'}} id="amount" type="text" ref={AmountValue => this.inputAmountValue = AmountValue} defaultValue="100"                    />提示:输入100表示充值1元</label>                </div>                <div>                    <label >                        <button onClick={this.handleSearch.bind(this)}>提交</button>                    </label>                </div>            </div>        )    }}//ReactDOM.render(//    <div>//        <Balances/>//    </div>, //   document.getElementById('balances')//)